#!/usr/bin/make -f

export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_install:
	$(MAKE) install \
		PREFIX=/usr \
		SYSCONFDIR=/etc \
		UNITDIR=/lib/systemd/system \
		DESTDIR=$(CURDIR)/debian/jool-netns
